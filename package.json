{
  "name": "katta",
  "version": "1.0.0",
  "description": "Full Stack Hangout Planner for Bengaluru",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
